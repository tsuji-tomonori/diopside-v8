interface:
  display_name: "品質ゲート検査"
  short_description: "Inspect only selected checks and current evidence"
  default_prompt: "Use $inspect-quality-gates to inspect the checks selected for this change and timing, list evidence-backed blockers, and require regulated lifecycle evidence only when the regulated profile is active."

interface:
  display_name: "チャットだけで自律開発"
  short_description: "Turn a casual request into a tested PR without user-run commands"
  default_prompt: "Use $chat-first-development to classify durable requirements, select the smallest sufficient profile, implement and verify the change, and prepare the tested PR; ask approval only at a real authority boundary."

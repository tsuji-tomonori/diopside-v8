interface:
  display_name: "永続要件の発見・保守"
  short_description: "対話から原子的な要件deltaを作り、正本と生成docsを維持する"
  default_prompt: "Use $maintain-canonical-requirements to discover my intent, separate atomic requirements, update the durable specification, and regenerate the human-readable requirements docs."
